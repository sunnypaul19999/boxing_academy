export function adminAcademyDeleteCardEvent(event, state, nav) {
    event.preventDefault();
    event.stopPropagation();
    //TODO: send delete req to server
}

export function adminCourseDeleteCardEvent(event, state, nav) {
    event.preventDefault();
    event.stopPropagation();
    //TODO: send delete req to server
}