export function cardUserOnEnrollCourseAction(event, state, nav) {
    event.preventDefault();
    event.stopPropagation();

    let cardProp = state.cardProp;
    //TODO: navigate user fill student details
}